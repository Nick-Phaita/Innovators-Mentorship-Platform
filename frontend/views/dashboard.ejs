<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="/assets/vendor/bootstrap/css/bootstrap.min.css">
    <!-- Your custom CSS -->
    <link rel="stylesheet" href="/assets/css/style.css">
</head>
<body>

    <!-- Sticky navigation bar -->
    <nav>
        <%- include('./includes/navbar.ejs', { user: user }) %>
    </nav>
    
    <!-- Main content -->
    <div class="container mt-4">
        <!-- Your dashboard content here -->
        <main>
            <section class="followed-projects">
                <h2 class="mb-4">Projects You Follow</h2>
                <div class="row">
                    <% followedProjects.forEach(project => { %>
                        <div class="col-md-4 mb-4">
                            <div class="card project-card" data-project-id="<%= project.project_id %>">
                                <div class="card-body">
                                    <h5 class="card-title"><%= project.title %></h5>
                                    <p class="card-text"><%= project.description %></p>
                                    <p class="card-text">Followers: <%= project.followersCount %></p>
                                    <a href="/project/<%= project.project_id %>" class="btn btn-primary">View Project</a>
                                    <button class="btn btn-danger follow-btn" data-project-id="<%= project.project_id %>">
                                        Unfollow
                                    </button>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                </div>
            </section>
        </main>
        <!-- Other dashboard content -->
    </div>

    <!-- Bootstrap JS and dependencies (jQuery and Popper.js) -->
    <script src="/assets/vendor/jquery/jquery.min.js"></script>
    <script src="/assets/vendor/popper.js/popper.min.js"></script>
    <script src="/assets/vendor/bootstrap/js/bootstrap.min.js"></script>
    <!-- Your JavaScript scripts -->
    <script src="/js/follow.js"></script>

</body>
</html>
