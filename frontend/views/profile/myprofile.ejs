<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Profile</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="/assets/vendor/bootstrap/css/bootstrap.min.css">
    <!-- Your custom CSS -->
    <link rel="stylesheet" href="/assets/css/style.css">
</head>
<body>
    <% if (message) { %>
        <script>
            alert("<%= message %>");
        </script>
    <% } %>
    <nav>
        <%- include('../includes/navbar.ejs', { user: user }) %>
    </nav>
    
    <div class="container mt-4">
        <div class="row">
            <aside class="col-md-3">
                <h4>Profile Navigation</h4>
                <ul class="list-group">
                    <li class="list-group-item"><a href="myprofile/authentication">Authentication Details</a></li>
                </ul>
            </aside>
            <main class="col-md-9">
                <h1>Welcome, <%= user.username %></h1>
                <p>Your role: <%= user.role %></p>
                <div class="card profile-card mt-3">
                    <div class="card-body">
                        <h2 class="card-title">Profile Details</h2>
                        <p><strong>First Name:</strong> <%= profile.first_name %></p>
                        <p><strong>Last Name:</strong> <%= profile.last_name %></p>
                        <p><strong>Occupation:</strong> <%= profile.occupation %></p>
                        <p><strong>Interests:</strong> <%= profile.interests %></p>
                        <p><strong>Skills:</strong> <%= profile.skills %></p>
                        <p><strong>Experience:</strong> <%= profile.experience %></p>
                        <p><strong>Achievements:</strong> <%= profile.achievements %></p>
                        <p><strong>Links:</strong> <%= profile.links %></p>
                        <p><strong>Bio:</strong> <%= profile.bio %></p>
                        <% if (user.user_id === profile.user_id) { %>
                            <a href="/profile/myprofile/details/update" class="btn btn-primary">Update Profile</a>
                        <% } %>
                    </div>
                </div>
            </main>
        </div>
    </div>

    <!-- Bootstrap JS and dependencies (jQuery and Popper.js) -->
    <script src="/assets/vendor/jquery/jquery.min.js"></script>
    <script src="/assets/vendor/popper.js/popper.min.js"></script>
    <script src="/assets/vendor/bootstrap/js/bootstrap.min.js"></script>
</body>
</html>
